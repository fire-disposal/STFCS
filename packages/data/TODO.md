# 前后端重构 TODO

## 概述
由于工程巨大，前后端代码暂不进行大规模重写。本文档列出未来重构任务，逐步适配 `data` 包的更改。

## 前端重构任务

### 1. 数据类型统一
- [ ] 将前端实体类迁移到使用 `data` 包中的 JSON 类型定义
- [ ] 更新 `renderer/entities/` 中的舰船、武器等类，使用 `ShipJSON`、`WeaponJSON` 等类型
- [ ] 替换硬编码的类型定义为 `data` 包导出

### 2. 配置管理重构
- [ ] 使用 `data/configs` 中的配置加载器替代前端的本地配置
- [ ] 更新 `services/SystemService.ts` 以使用 `getGameRules()` 和 `getServerConfig()`
- [ ] 移除前端的重复配置，统一从 `data` 包加载

### 3. 数据同步优化
- [ ] 在 `sync/` 模块中使用 `DataRegistry` 进行数据验证和缓存
- [ ] 更新 `useMultiplayerState.ts` 等 hooks 以适配新的数据结构
- [ ] 实现增量同步机制，减少网络传输

### 4. UI 组件适配
- [ ] 更新 `ui/panels/` 中的组件以使用新的数据类型
- [ ] 优化渲染性能，使用 `data` 包的运行时常量
- [ ] 添加数据验证层，防止无效数据渲染

## 后端重构任务

### 1. DTO 和验证重构
- [ ] 更新 `server/src/dto/` 中的数据传输对象，使用 `data` 包的类型
- [ ] 在 `validation/` 模块中使用 `DataRegistry` 进行数据验证
- [ ] 统一错误处理，使用 `data` 包的错误类型定义

### 2. 命令处理优化
- [ ] 重构 `commands/` 模块，使用 `WeaponJSON` 和 `ShipJSON` 进行逻辑处理
- [ ] 更新房间管理 (`rooms/`) 以使用新的配置加载器
- [ ] 实现命令缓存机制，减少重复计算

### 3. 数据库适配
- [ ] 更新数据库 schema 以匹配 `data` 包的数据结构
- [ ] 在 `db/` 模块中使用 `DataRegistry` 进行数据持久化
- [ ] 添加数据迁移脚本，逐步过渡到新结构

### 4. 性能优化
- [ ] 使用 `data` 包的预设缓存服务优化后端性能
- [ ] 实现数据预加载机制，减少运行时查询
- [ ] 添加监控和日志，使用 `data` 包的类型进行结构化日志

## 通用任务

### 1. 测试覆盖
- [ ] 为 `data` 包的使用添加单元测试
- [ ] 更新集成测试以验证前后端数据一致性
- [ ] 添加性能基准测试

### 2. 文档更新
- [ ] 更新 API 文档以反映新的数据结构
- [ ] 创建迁移指南，帮助开发者理解更改
- [ ] 添加代码示例和最佳实践

### 3. 部署和监控
- [ ] 更新 CI/CD 流水线以包含 `data` 包的构建
- [ ] 添加数据一致性检查工具
- [ ] 实现渐进式部署策略

## 优先级和时间线
- **高优先级**: 数据类型统一、配置管理重构
- **中优先级**: 数据同步优化、DTO 验证重构
- **低优先级**: 性能优化、测试覆盖

逐步实施，每完成一个模块进行集成测试，确保系统稳定性。