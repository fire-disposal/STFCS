{
  "name": "@vt/electron",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "dev": "tsup && electron .",
    "dev:local": "tsup && electron . --local",
    "build": "tsup",
    "package": "pnpm build && electron-builder --win --config electron-builder.yml"
  },
  "devDependencies": {
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "tsup": "^8.5.1",
    "typescript": "~5.9.3"
  }
}
